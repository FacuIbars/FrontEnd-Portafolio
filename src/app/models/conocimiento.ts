export class Conocimiento {
    id?: number;
    lenguaje: string;
    skill: number;
    fotos?: string;
    constructor(id: number, lenguaje: string, skill: number, fotos: string) {
this.id = id
this.lenguaje = lenguaje
this.skill = skill
this.fotos = fotos

    }
}